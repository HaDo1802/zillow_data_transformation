x-airflow-common: &airflow-common
  build:
    context: ./docker/airflow
  image: real-estate-airflow:local
  env_file:
    - .env
  environment:
    AIRFLOW__CORE__EXECUTOR: LocalExecutor
    AIRFLOW__CORE__LOAD_EXAMPLES: "False"
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "True"
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${AIRFLOW_POSTGRES_USER}:${AIRFLOW_POSTGRES_PASSWORD}@airflow-postgres/${AIRFLOW_POSTGRES_DB}
    AIRFLOW__WEBSERVER__EXPOSE_CONFIG: "False"
    AIRFLOW_UID: ${AIRFLOW_UID:-50000}
    REPO_ROOT: /opt/airflow/project
    DBT_PROJECT_DIR: /opt/airflow/project/zillow_transformation
    DBT_PROFILES_DIR: /opt/airflow/project/zillow_transformation
    SUPABASE_SCHEMA: ${SUPABASE_SCHEMA:-raw}
    SUPABASE_RAW_TABLE: ${SUPABASE_RAW_TABLE:-raw_property_master_data}
  volumes:
    - ./airflow/dags:/opt/airflow/dags
    - ./logs:/opt/airflow/logs
    - ./zillow_transformation:/opt/airflow/project/zillow_transformation
    - ./scripts:/opt/airflow/project/scripts
    - ./.env:/opt/airflow/project/.env:ro
  extra_hosts:
    - "host.docker.internal:host-gateway"
  user: "${AIRFLOW_UID:-50000}:0"
  depends_on:
    airflow-postgres:
      condition: service_healthy

services:
  airflow-postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ${AIRFLOW_POSTGRES_USER}
      POSTGRES_PASSWORD: ${AIRFLOW_POSTGRES_PASSWORD}
      POSTGRES_DB: ${AIRFLOW_POSTGRES_DB}
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${AIRFLOW_POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - airflow_postgres_data:/var/lib/postgresql/data

  airflow-init:
    <<: *airflow-common
    command: >
      bash -c "airflow db migrate && airflow users create
      --role Admin
      --username $${AIRFLOW_WWW_USER_USERNAME:-admin}
      --password $${AIRFLOW_WWW_USER_PASSWORD:-admin}
      --firstname Airflow
      --lastname Admin
      --email admin@example.com"
    restart: "no"

  airflow-webserver:
    <<: *airflow-common
    command: webserver
    depends_on:
      airflow-postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully
    ports:
      - "8080:8080"
    restart: unless-stopped

  airflow-scheduler:
    <<: *airflow-common
    command: scheduler
    depends_on:
      airflow-postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  airflow_postgres_data:
