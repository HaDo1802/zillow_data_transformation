version: 2

sources:
  - name: raw
    description: Raw Zillow property snapshot data loaded from Supabase Storage (raw landing layer)
    schema: raw
    tables:
      - name: raw_property_master_data
        description: Raw CSV snapshots of Zillow property listings
        loaded_at_field: ingested_time
